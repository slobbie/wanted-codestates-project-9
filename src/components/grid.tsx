import styled from 'styled-components';

const Grid = () => {
  return (
    <Section>
      <h2>grid</h2>
    </Section>
  );
};
export default Grid;

const Section = styled.section`
  width: 100%;
  height: 100vw;
  background-color: #fff; ;
`;
